{% trans_default_domain 'FOSUserBundle' %}
    {% stylesheets 'bundles/user/css/register.css' output='compiled/css/register.css' filter='cssrewrite' %}
<link href="{{ asset_url }}" rel="stylesheet">

{% endstylesheets %}

{% block body %}
<div class="container">
    <div class="center">
        <h1 class="text-uppercase text-center" >découvre nos équipes de folies</h1>
        <h3 class="text-uppercase text-center tinder">*le tinder de la sortie culturelle*</h3>

{{ form_start(form, {'method': 'post', 'action': path('fos_user_registration_register'), 'attr': {'class': 'fos_user_registration_register'}}) }}
    {{ form_widget(form) }}
<div>
    <input type="submit" value="{{ 'registration.submit'|trans }}" class="color-button btn btn-default button" />
</div>
{{ form_end(form) }}
        <a href="">J'ai déja un compte !</a>
    </div>

</div>

{% endblock %}

